<ul class="list-group list-style-none">
    {% for link in items %}
        <li class="list-group-item border-0 p-0">
            {% if isProduct %}
                <a
                        class="small"
                        href="{{ path( link.is_offer == true ? 'products-product:public.model' : 'products-product:public.detail', {
                            'category': link.category_url,
                            'url': link.url
                        }) }}"
                >
                    {{ link.name | raw }}
                </a>

            {% else %}

                <a
                        class=" small"
                        href="{{ path('products-product:public.catalog.category', {'category': link.url}) }}"
                >
                    {{ link.name }}
                </a>

            {% endif %}

        </li>


    {% endfor %}

</ul>


{# fregdfg #}
