<?xml version="1.0"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {# Категори продукции #}
    {% for url in urls %}
        <url>
            <loc>{{ absolute_url(path('products-product:user.catalog.category', { category: url.category_url } )) }}</loc>
        </url>
    {% endfor %}
</urlset>
